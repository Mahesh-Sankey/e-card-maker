import { View } from 'react-native';
import MuttonScreen from '../commonComponents/CollapsibleCard';

export const MuttonPage = (props) => {
    return (
        <View>
            <MuttonScreen />
        </View>
    );
};