import { View } from 'react-native';
import { ChickenScreen } from '../commonComponents/CollapsibleCard';

export const ChickenPage = (props) => {
    return (
        <View>
            <ChickenScreen />
        </View>
    );
};