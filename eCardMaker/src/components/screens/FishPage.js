import { View } from 'react-native';
import { FishScreen } from '../commonComponents/CollapsibleCard';

export const FishPage = (props) => {
    return (
        <View>
            <FishScreen/>
        </View>
    );
};