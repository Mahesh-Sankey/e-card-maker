import { View,Text } from 'react-native';
import { VegScreen } from '../commonComponents/CollapsibleCard';

export const VegPage = (props) => {
    return (
        <View>
            <VegScreen/>
        </View>
    );
};